import * as React from 'react';
import { render } from 'react-dom';
import './style.scss';

const b = { a: 1 };

const app = () => (
    <div>
        <h1>
            123ff
        </h1>
    </div>
);

render(app(), document.getElementById('root'));
